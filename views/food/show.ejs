<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= foundFood.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="container">
      <h1><%= foundFood.name %></h1>
      <p><strong>Category:</strong> <%= foundFood.category %></p>
      <p><strong>Price:</strong> $<%= foundFood.price %></p>
      <p><strong>Description:</strong> <%= foundFood.description %></p>

      <div class="actions">
        <form action="/food/<%= foundFood._id %>?_method=DELETE" method="POST">
          <button type="submit" class="btn danger">Delete</button>
        </form>
        <a href="/food/<%= foundFood._id %>/edit" class="btn">Edit</a>
        <a href="/food" class="btn">Back to All Food</a>
      </div>
    </div>
  </body>
</html>
